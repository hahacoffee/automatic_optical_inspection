#???????????
#???
clc()
import numpy as np
R=20 #???? 20 mm
N=1000 #??1000??
x=np.linspace(-R/2,R/2,N) # ??x???
X,Y=np.meshgrid(x,x) # ??
f1=1 # 1/mm?grating ?????(?????(1mm)???)
q=0*np.pi/180 # ??(??)
c=np.cos(q)
s=np.sin(q)
Z=dataObject(1+np.cos(2*np.pi*f1*X)) #? x ??grating

E=np.exp(-X**2/5-Y**2/5)
E=np.where(E>0.2,1.0,0)
E=dataObject(E)

ZE=Z.mul(E) #dataObject???

Z.setAxisScale(1,R/N) #???(x?)
Z.setAxisScale(0,R/N) #???(y?)
ZEf=dataObject()
filter("fftw2D",ZE,ZEf)
filter("fftshift",ZEf)