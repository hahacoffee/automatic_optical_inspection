#???
clc()
import numpy as np
R=20 #???? 20 mm
N=1000 #??1000??
x=np.linspace(-R/2,R/2,N) # ??x???
X,Y=np.meshgrid(x,x) # ??
f1=1 # 1/mm?grating ?????(?????(1mm)???)
f2=3

Z=dataObject(1+np.cos(2*np.pi*f1*X)+np.cos(2*np.pi*f1*Y)) #? x ??grating

Z.setAxisScale(1,R/N) #???(x?)
Z.setAxisScale(0,R/N) #???(y?)
Zf=dataObject()
filter("fftw2D",Z,Zf)
filter("fftshift",Zf)